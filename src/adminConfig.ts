// Admin identity for the app.
// The account with this email can approve new signups in the Admin Panel.
// Keep in sync with isAdmin() in firestore.rules.
export const ADMIN_EMAIL = 'mshanir@gmail.com';
